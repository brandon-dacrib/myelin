# Generated by `cargo run -p hs-operator --bin gen-crds`. Do not hand-edit; change the CRD's Rust struct in crates/hs-operator/src/crds/homeserver.rs and regenerate.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: homeservers.hs.matrix.org
spec:
  group: hs.matrix.org
  names:
    categories: []
    kind: Homeserver
    plural: homeservers
    shortNames:
    - hs
    singular: homeserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.serverName
      name: ServerName
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for HomeserverSpec via `CustomResource`
        properties:
          spec:
            description: '`spec` of a `Homeserver`.'
            properties:
              extraConfig:
                additionalProperties: true
                description: Extra native `hs-config` YAML, appended to the operator-generated `ConfigMap` the same way `deploy/helm/hs/values.yaml`'s `extraConfig` does.
                type: object
              image:
                description: The `hs` image to run.
                properties:
                  digest:
                    description: An image digest (`sha256:...`), for a pinned, content-addressed reference.
                    nullable: true
                    type: string
                  pull_policy:
                    description: Pull policy. Defaults to the cluster/Kubernetes default (`IfNotPresent` for a tag, `Always` otherwise) when omitted, same as `deploy/helm/hs`.
                    nullable: true
                    type: string
                  repository:
                    description: The image repository, e.g. `ghcr.io/matrix-org/hs`.
                    type: string
                  tag:
                    description: The image tag. Mutually exclusive with `digest` in principle; both are accepted and `digest` wins if both are set (the same convention `deploy/helm/hs`'s `hs.image` template helper uses).
                    nullable: true
                    type: string
                required:
                - repository
                type: object
              publicBaseUrl:
                description: The externally reachable base URL, if different from `https://{serverName}`.
                nullable: true
                type: string
              registrationSharedSecretRef:
                description: '`auth.registration_shared_secret`, from an existing `Secret`.'
                nullable: true
                properties:
                  key:
                    description: The key within that `Secret`'s `data`/`stringData`.
                    type: string
                  name:
                    description: The `Secret` name.
                    type: string
                required:
                - key
                - name
                type: object
              replicas:
                default: 1
                description: Number of replicas. Forced to `1` by the reconciler when `storage` is `Embedded`.
                format: int32
                type: integer
              serverName:
                description: The Matrix server name (`server.server_name`). Immutable after creation in practice (changing it after any room exists is not supported by any Matrix homeserver — see `hs_config::server::ServerConfig`'s doc comment); the reconciler does not enforce immutability itself (no Kubernetes CRD-level immutability marker for this yet), but the change would need a full new deployment in practice.
                type: string
              signingKeySecretRef:
                description: The Ed25519 signing key (`hs generate-signing-key` format), from an existing `Secret`. Required — the operator never generates or stores a signing key itself, matching `deploy/helm/hs`'s same policy (a signing key must survive the CR being deleted and recreated).
                properties:
                  key:
                    description: The key within that `Secret`'s `data`/`stringData`.
                    type: string
                  name:
                    description: The `Secret` name.
                    type: string
                required:
                - key
                - name
                type: object
              storage:
                description: Storage backend.
                properties:
                  backend:
                    description: Which of `embedded`/`postgres`/`slatedb` below is meaningful.
                    enum:
                    - embedded
                    - postgres
                    - slatedb
                    type: string
                  embedded:
                    description: 'Meaningful when `backend: embedded`.'
                    nullable: true
                    properties:
                      size:
                        description: Requested PVC size, e.g. `"10Gi"`.
                        type: string
                      storageClassName:
                        description: '`StorageClass` name; the cluster default is used if omitted.'
                        nullable: true
                        type: string
                    required:
                    - size
                    type: object
                  postgres:
                    description: 'Meaningful when `backend: postgres`.'
                    nullable: true
                    properties:
                      cloudNativePgCluster:
                        description: A CloudNativePG `Cluster` name in the same namespace, to read connection details from its generated `<name>-app` Secret. Mutually exclusive with `host` in practice (the reconciler prefers `cloud_native_pg_cluster` when both are set).
                        nullable: true
                        type: string
                      database:
                        description: Database name.
                        nullable: true
                        type: string
                      host:
                        description: An externally managed PostgreSQL host, for a non-CloudNativePG database.
                        nullable: true
                        type: string
                      passwordSecretRef:
                        description: The Secret key holding the connection password.
                        nullable: true
                        properties:
                          key:
                            description: The key within that `Secret`'s `data`/`stringData`.
                            type: string
                          name:
                            description: The `Secret` name.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      user:
                        description: Connecting role.
                        nullable: true
                        type: string
                    type: object
                  slatedb:
                    description: 'Meaningful when `backend: slatedb`.'
                    nullable: true
                    properties:
                      bucketUrl:
                        description: The object store URL (`s3://bucket/prefix`, ...).
                        type: string
                      shardCount:
                        default: 256
                        description: Number of virtual storage shards.
                        format: uint32
                        minimum: 0.0
                        type: integer
                    required:
                    - bucketUrl
                    type: object
                required:
                - backend
                type: object
            required:
            - image
            - serverName
            - signingKeySecretRef
            - storage
            type: object
          status:
            description: 'The status sub-resource every kind in this operator uses. Identical shape across all five kinds by design: one dashboard query, one `kubectl get -o jsonpath=''{.status.phase}''` habit, works against any of them.'
            nullable: true
            properties:
              conditions:
                default: []
                description: Standard Kubernetes conditions (`type`, `status`, `reason`, `message`, `lastTransitionTime`). Empty until the first reconcile.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observed_generation:
                description: The `.metadata.generation` this status was computed from, for a caller to tell whether `status` reflects the current `spec` or a stale one the controller has not caught up to yet (the standard Kubernetes "observedGeneration" convention).
                format: int64
                nullable: true
                type: integer
              phase:
                default: Pending
                description: Coarse lifecycle phase.
                enum:
                - Pending
                - Ready
                - Degraded
                type: string
              ready_replicas:
                description: How many replicas of the managed workload are currently ready.
                format: int32
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: Homeserver
        type: object
    served: true
    storage: true
    subresources:
      status: {}
