# Generated by `cargo run -p hs-operator --bin gen-crds`. Do not hand-edit; change the CRD's Rust struct in crates/hs-operator/src/crds/appservice.rs and regenerate.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: appservices.hs.matrix.org
spec:
  group: hs.matrix.org
  names:
    categories: []
    kind: AppService
    plural: appservices
    shortNames:
    - as
    singular: appservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.homeserverRef
      name: Homeserver
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AppServiceSpec via `CustomResource`
        properties:
          spec:
            description: '`spec` of an `AppService`.'
            properties:
              asTokenSecretRef:
                description: '`Secret` key holding the appservice''s own token (used by the appservice to authenticate to the homeserver, `as_token`).'
                properties:
                  key:
                    description: The key within that `Secret`'s `data`/`stringData`.
                    type: string
                  name:
                    description: The `Secret` name.
                    type: string
                required:
                - key
                - name
                type: object
              homeserverRef:
                description: The name of the [`super::homeserver::Homeserver`] this registration applies to (same namespace).
                type: string
              hsTokenSecretRef:
                description: '`Secret` key holding the homeserver''s token (used by the homeserver to authenticate to the appservice, `hs_token`).'
                properties:
                  key:
                    description: The key within that `Secret`'s `data`/`stringData`.
                    type: string
                  name:
                    description: The `Secret` name.
                    type: string
                required:
                - key
                - name
                type: object
              id:
                description: The appservice's stable ID (Synapse's/this project's `id` registration field).
                type: string
              namespaces:
                default:
                  users: []
                  aliases: []
                  rooms: []
                description: Namespaces this appservice claims.
                properties:
                  aliases:
                    default: []
                    description: Room alias regexes.
                    items:
                      description: One namespace regex entry.
                      properties:
                        exclusive:
                          default: false
                          description: Whether this appservice exclusively owns matching IDs (no other appservice or local user may claim one).
                          type: boolean
                        regex:
                          description: The regular expression.
                          type: string
                      required:
                      - regex
                      type: object
                    type: array
                  rooms:
                    default: []
                    description: Room ID regexes.
                    items:
                      description: One namespace regex entry.
                      properties:
                        exclusive:
                          default: false
                          description: Whether this appservice exclusively owns matching IDs (no other appservice or local user may claim one).
                          type: boolean
                        regex:
                          description: The regular expression.
                          type: string
                      required:
                      - regex
                      type: object
                    type: array
                  users:
                    default: []
                    description: User ID regexes this appservice owns.
                    items:
                      description: One namespace regex entry.
                      properties:
                        exclusive:
                          default: false
                          description: Whether this appservice exclusively owns matching IDs (no other appservice or local user may claim one).
                          type: boolean
                        regex:
                          description: The regular expression.
                          type: string
                      required:
                      - regex
                      type: object
                    type: array
                type: object
              rateLimited:
                default: false
                description: Whether this appservice is subject to the homeserver's normal rate limiting.
                type: boolean
              senderLocalpart:
                description: The localpart of the appservice's sender user (`@<sender_localpart>:server_name`).
                type: string
              url:
                description: The URL this server pushes events to.
                type: string
            required:
            - asTokenSecretRef
            - homeserverRef
            - hsTokenSecretRef
            - id
            - senderLocalpart
            - url
            type: object
          status:
            description: 'The status sub-resource every kind in this operator uses. Identical shape across all five kinds by design: one dashboard query, one `kubectl get -o jsonpath=''{.status.phase}''` habit, works against any of them.'
            nullable: true
            properties:
              conditions:
                default: []
                description: Standard Kubernetes conditions (`type`, `status`, `reason`, `message`, `lastTransitionTime`). Empty until the first reconcile.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observed_generation:
                description: The `.metadata.generation` this status was computed from, for a caller to tell whether `status` reflects the current `spec` or a stale one the controller has not caught up to yet (the standard Kubernetes "observedGeneration" convention).
                format: int64
                nullable: true
                type: integer
              phase:
                default: Pending
                description: Coarse lifecycle phase.
                enum:
                - Pending
                - Ready
                - Degraded
                type: string
              ready_replicas:
                description: How many replicas of the managed workload are currently ready.
                format: int32
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: AppService
        type: object
    served: true
    storage: true
    subresources:
      status: {}
