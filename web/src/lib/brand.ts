/**
 * What this product calls itself, in one place.
 *
 * The interface said "hs admin" long after the project became Myelin, because the name was
 * typed into each component that showed it (`hs` was the placeholder the crates still carry).
 * `index.html`'s `<title>` cannot import this and says the same thing by hand.
 */
export const PRODUCT_NAME = "Myelin";
